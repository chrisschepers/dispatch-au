{
  "name": "dispatch-feed-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --experimental-strip-types server.mjs",
    "test": "node --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "@nodable/entities": "^3.0.0",
    "fast-xml-parser": "^5.11.1",
    "fast-xml-validator": "^1.4.2"
  }
}
